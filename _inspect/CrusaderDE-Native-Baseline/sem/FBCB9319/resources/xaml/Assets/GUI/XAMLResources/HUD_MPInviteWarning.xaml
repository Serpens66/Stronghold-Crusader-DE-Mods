<UserControl
    x:Class="CrusaderDE.HUD_MPInviteWarning"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CrusaderDE"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="140"
    d:DesignWidth="620"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />

    </UserControl.Resources>

    <Grid
        x:Name="LayoutRoot"
        Width="620"
        Height="140"
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        Background="#00000000"
        UseLayoutRounding="True">
        <Grid.RenderTransform>
            <TranslateTransform />
        </Grid.RenderTransform>




        <Button
            Width="620"
            Height="140"
            Margin="0,0,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Style="{StaticResource BDR_SH1DEb}" />

        <TextBlock
                    Width="568"
                    Margin="0,17,0,0"
                    HorizontalAlignment="Center"
                    FontSize="22"
                    Foreground="#FF000000"
                    VerticalAlignment="Top"
                    TextAlignment="Center"
                    Text="{Binding Source={x:Static local:Translate.Instance}, Path=GameTexts[TEXT_NEW_TEXT_040]}"/>
        <TextBlock
                    Width="568"
                    Margin="0,50,0,50"
                    HorizontalAlignment="Center"
                    FontSize="18"
                    Foreground="#FF000000"
                    VerticalAlignment="Top"
                    TextAlignment="Center"
                    TextWrapping="WrapWithOverflow"
                    Text="{Binding Source={x:Static local:Translate.Instance}, Path=GameTexts[TEXT_NEW_TEXT_041]}"/>
        <Button
                    Name="IgnoreButton"
                    Width="183"
                    Margin="0,0,44,26"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    local:PropEx.TextCentre="{Binding Source={x:Static local:Translate.Instance}, Path=GameTexts[TEXT_NEW_TEXT_042]}"
                    local:PropEx.TextLeft=""
                    local:PropEx.TextRight=""
                    Command="{Binding ButtonMPInviteCommand}"
                    MouseEnter="CommonRedButtonEnter"
                    Style="{StaticResource BTN_SH_GlowS}" />
    </Grid>

</UserControl>