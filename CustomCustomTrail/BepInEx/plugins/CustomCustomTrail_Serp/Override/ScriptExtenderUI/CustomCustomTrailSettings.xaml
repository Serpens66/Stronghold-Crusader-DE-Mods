<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:shared="clr-namespace:Shared;assembly=CustomCustomTrail">
  <Grid.Resources>
    <Style x:Key="ModSettingsToolTipStyle" TargetType="{x:Type ToolTip}">
      <Setter Property="Background" Value="#FF1D1710"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontSize" Value="{x:Static shared:ToolTipPresentation.FontSize}"/>
      <Setter Property="BorderBrush" Value="#FFF2D48A"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ToolTip}">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2" Padding="10,7" MaxWidth="{x:Static shared:ToolTipPresentation.MaximumWidth}">
              <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}" TextWrapping="Wrap"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="{x:Type ToolTip}" BasedOn="{StaticResource ModSettingsToolTipStyle}"/>
    <Style x:Key="ClientActivationBorder" TargetType="{x:Type Border}"><Setter Property="Background" Value="#44306950"/><Setter Property="BorderBrush" Value="#FF66CC99"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="3"/><Setter Property="Padding" Value="8,4"/></Style>
    <Style x:Key="HostOptionsBorder" TargetType="{x:Type Border}"><Setter Property="Background" Value="#443E315F"/><Setter Property="BorderBrush" Value="#FFB99BE8"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="3"/><Setter Property="Padding" Value="10,8"/></Style>
  </Grid.Resources>
  <ScrollViewer HorizontalScrollBarVisibility="{x:Static shared:ToolTipPresentation.AutomaticScrollBarVisibility}"
                VerticalScrollBarVisibility="{x:Static shared:ToolTipPresentation.AutomaticScrollBarVisibility}">
    <StackPanel Margin="10">
      <Border Style="{StaticResource ClientActivationBorder}" HorizontalAlignment="Left">
        <CheckBox IsChecked="{Binding EnableMod, Mode=TwoWay}"
                  Content="{Binding EnableModText}"
                  ToolTipService.ShowDuration="60000"
                  ToolTip="{Binding EnableModHelpText}"
                  Foreground="White"
                  FontWeight="Bold"/>
      </Border>
      <TextBlock Text="{Binding PracticalEffectsText}"
                 Foreground="#FFDDDDDD"
                 TextWrapping="Wrap"
                 Margin="0,10,0,0"/>
      <TextBlock Text="{Binding HostOptionsText}"
                 Foreground="#FFD1B8FF"
                 FontWeight="Bold"
                 Margin="0,20,0,7"/>
      <Border Style="{StaticResource HostOptionsBorder}" HorizontalAlignment="Left">
        <StackPanel>
          <TextBlock Text="{Binding CoopPackageText}" Foreground="White" Margin="0,0,0,4"/>
          <ComboBox ItemsSource="{Binding CoopPackageOptions}"
                    SelectedItem="{Binding SelectedCoopPackage, Mode=TwoWay}"
                    IsEnabled="{Binding CanEditCoopPackage}"
                    Width="520"
                    HorizontalAlignment="Left"
                    ToolTipService.ShowDuration="60000"
                    ToolTip="{Binding CoopPackageHelpText}"/>
          <TextBlock Text="{Binding HostReadOnlyNoticeText}"
                     Visibility="{Binding HostReadOnlyNoticeVisibility}"
                     Foreground="#FFFFCC66"
                     Margin="0,7,0,0"/>
          <StackPanel Orientation="Horizontal" Margin="0,7,0,0">
            <TextBlock Text="{Binding CoopPackageStatusLabel}" Foreground="#FFCCCCCC"/>
            <TextBlock Text="{Binding CoopPackageStatusText}" Foreground="White" TextWrapping="Wrap" MaxWidth="700" Margin="5,0,0,0"/>
          </StackPanel>
        </StackPanel>
      </Border>
      <TextBlock Text="{Binding SupportedTrailSettingsTitle}"
                 Foreground="#FFF2D48A"
                 FontWeight="Bold"
                 Margin="0,16,0,4"/>
      <TextBlock Text="{Binding SupportedTrailSettingsHelpText}"
                 Foreground="#FFCCCCCC"
                 TextWrapping="Wrap"/>
      <ItemsControl ItemsSource="{Binding CompatibleTrailMods}" Margin="12,6,0,0">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                      Content="{Binding DisplayName}"
                      Foreground="White"
                      ToolTipService.ShowDuration="60000"
                      Margin="0,2">
              <CheckBox.ToolTip>
                <ToolTip Style="{StaticResource ModSettingsToolTipStyle}"
                         Content="{Binding HelpText}"/>
              </CheckBox.ToolTip>
            </CheckBox>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
      <StackPanel Visibility="{Binding IncompatibleTrailModsVisibility}" Margin="0,12,0,0">
        <TextBlock Text="{Binding IncompatibleTrailModsLabel}"
                   Foreground="#FFFFCC66"
                   FontWeight="Bold"/>
        <TextBlock Text="{Binding IncompatibleTrailModsText}"
                   Foreground="#FFCCCCCC"
                   TextWrapping="Wrap"
                   Margin="12,4,0,0"/>
      </StackPanel>
      <TextBlock Margin="0,10,0,0">
        <Hyperlink Command="{Binding OpenCompatibilityGuideCommand}"
                   ToolTipService.ShowDuration="60000"
                   ToolTip="{Binding CompatibilityGuideHelpText}">
          <Run Text="{Binding CompatibilityGuideText}"/>
        </Hyperlink>
      </TextBlock>
    </StackPanel>
  </ScrollViewer>
</Grid>
