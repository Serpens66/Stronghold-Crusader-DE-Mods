Was ment to add optional speed for move command of units:
- Ctrl+Click : every unit moves at their max speed.
- Alt+Click : every unit moves at max speed of slowest unit

Not needed because:
- this can already achieved in vanilla: a group already moves at lowest speed together
- when clicking on any unit on the selection on bottom of the screen, they all start to move at max speed

Only problems are:
- Spearmen with the faster spearmen setting do move faster when in mixed group
- When selecting the already running group again in a mixed way, they will slow down again